import React from 'react';
import Board from './Board';

export default React.createClass({
    render: function() {
        return (
            <div class="container">
                <Board></Board>
            </div>
        );
    }
});